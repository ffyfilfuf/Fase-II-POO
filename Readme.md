# Markdown
- Introdução
- Segundo
- Terceiro

## Lista numerada

1. Primeiro
1. Segundo
1. Terceiro

## Trechos de códigos

```bash
# Criando diretório
mkdir primeiro

cd primeiro

# Iniciando repositório
git init

git config --global user.name "Arthur de Oliveira"

git config --global user.email "arthur.o08@aluno.ifsc.edu.br"

git config --global alias.tree "log --oneline --graph --decorate --all"

git config --global credential.helper 'cache --timeout=3600'

git commit -m "mensagem para o commit"

git remote add origin https://github.com/ffyfilfuf/Fase-II-POO.git


git checkout
```

### Olá mundo em Java

```java
public class Ola{
    public static void main String[] args {
        System.out.println("Olá Mundo!")
    }
}
```
